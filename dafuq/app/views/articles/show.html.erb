<%= link_to "All the articles", controller: 'articles' %>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= raw(@article.text) %>
</p>

<br>

<h2>Comments</h2>
<%= render @article.comments %>


<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
<!-- If you want to link to an action in the same controller, -->
<!-- you don't need to specify the :controller option, -->
<!-- as Rails will use the current controller by default. -->
<!-- per: http://guides.rubyonrails.org/v4.2/getting_started.html#adding-links -->
